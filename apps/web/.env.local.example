# Backend (signaling/SFU) URL, read at RUNTIME by the Next.js server and served
# to the browser via /api/config. Not baked into the build — so the same image
# works anywhere; just change this env var per environment.
SERVER_URL=http://localhost:4000
